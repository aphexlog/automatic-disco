# create a buildspec.yml file
version: 1
env:
  variables:
    BACKEND_ENV: "staging"
frontend:
  phases:
    preBuild:
      commands:
        - amplify env checkout staging
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
